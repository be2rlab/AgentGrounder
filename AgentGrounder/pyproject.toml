[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pc_grounder"
version = "1.0.0"
description = "PC-Grounder: Zero-Shot 3D Visual Pointcloud Grounding"
readme = ""
dependencies = []

[tool.setuptools]
packages = { find = {} }